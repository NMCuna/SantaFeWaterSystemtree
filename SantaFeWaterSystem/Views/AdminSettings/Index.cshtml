@model SantaFeWaterSystem.Models.AdminAccessSetting
@{
    ViewData["Title"] = "Admin Token";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2 class="mb-0"><i class="bi bi-shield-lock"></i> Admin Login Token</h2>
        <a asp-controller="GeneralSettings" asp-action="Security" class="btn btn-secondary">
            <i class="bi bi-arrow-left-circle me-1"></i> Back
        </a>
    </div>

    <!-- 🔥 Strong Reminder -->
    <div class="alert alert-danger" role="alert">
        <i class="bi bi-exclamation-triangle-fill"></i>
        <strong>Keep this token safe!</strong> Losing or forgetting it will lock you out of the Admin Login page.
    </div>

    <div class="card mt-3">
        <div class="card-body">
            <p><strong>Current Token:</strong> <code>@Model.LoginViewToken</code></p>
            <a asp-action="EditToken" class="btn btn-warning">
                <i class="bi bi-pencil"></i> Edit Token
            </a>
        </div>
    </div>

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success mt-3">@TempData["Success"]</div>
    }

    <!-- 🔹 Info Panel -->
    <div class="mt-4">
        <div class="alert alert-secondary small">
            <i class="bi bi-info-circle me-1"></i>
            <strong>What is the Admin Login Token?</strong><br />
            The Admin Login Token is an extra security step for logging into the system’s admin area.
            Only users who know this token can reach the Admin Login page.

            <hr class="my-2" />

            <strong><i class="bi bi-shield-check me-1"></i> Why it is used:</strong>
            <ul class="mb-2">
                <li>Prevents unauthorized users from even seeing the Admin Login screen.</li>
                <li>Adds a secret layer on top of the normal username and password.</li>
                <li>Stops bots or attackers from brute-forcing the login page directly.</li>
            </ul>

            <strong><i class="bi bi-gear me-1"></i> How it works:</strong>
            <ul class="mb-0">
                <li>Before reaching the Admin Login page, the system asks for the token.</li>
                <li>If the token is wrong, access is blocked immediately.</li>
                <li>Only admins who know the token can continue to enter their username and password.</li>
            </ul>

            <hr class="my-2" />

            <i class="bi bi-lightbulb me-1"></i>
            <em>Tip: Change the token periodically and share it only with trusted admins and staff.</em>
        </div>
    </div>
</div>
